define(function () {
    /**
     * Vector class.
     * Define a vector coordinate (X, Y)
     */
    class Vector {
        constructor({ x, y }) {
            this.x = x
            this.y = y
        }
    }
    return Vector
})