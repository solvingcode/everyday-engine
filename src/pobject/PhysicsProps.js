define(function(){

    class PhysicsProps{

        velocity

        /**
         * @param {number} velocity
         */
        setVelocity(velocity){
            this.velocity = velocity
        }

    }

    return PhysicsProps

})